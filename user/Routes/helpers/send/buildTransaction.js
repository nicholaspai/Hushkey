

const buildTransaction = async (txInfo, chain) => {
    switch(chain) {
        case "BTC":
            // build and return BTC transaction
            break;
        case "ETH":
            // build and return ETH transaction
            break;
        case "EOS":
            // build and return EOS transaction
            break;
        case "TRX":
            // build and return TRX transaction
            break;
    }
}

module.exports = { 
    buildTransaction
}